import React from "react";
//import { useEffect, useState } from "react";

function Button1 ({setters, values}) {
    const {buttonone} = values;
    const {setButtonOne} = setters;

    return (
        <div>
            <button> onClick={Button1}</button>
        </div>
    )

}